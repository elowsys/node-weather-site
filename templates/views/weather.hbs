<!DOCTYPE html>
<html>

<head>
    <title>{{title}}</title>
    <link href="./css/styles.css" rel="stylesheet" />
    <link rel="icon" href="/img/weather.png">
</head>

<body>
    <div class="main-content">
        {{>header}}
        {{!-- <p class="text-muted">{{data.forecast}} in {{data.location}}</p> --}}
        <p class="text-muted">{{data.location}}</p>
        <p class="text-muted">{{data.summary}}</p>
        <p class="text-muted">{{data.lowHigh}}</p>
        <p class="text-muted">{{data.condition}}</p>
        <p class="text-muted">{{data.chance}}</p>
    </div>
    {{>footer}}
</body>

</html>
